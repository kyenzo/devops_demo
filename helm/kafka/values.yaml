# Strimzi Kafka Operator configuration
# Installs the operator that manages Kafka clusters

# Watch all namespaces for Kafka resources
watchAnyNamespace: true

# Resource limits for the operator
resources:
  limits:
    cpu: 500m
    memory: 512Mi
  requests:
    cpu: 200m
    memory: 256Mi
